# Generative AI Exercises

Projects related to
[generative AI and model exercises](https://microsoftlearning.github.io/mslearn-ai-studio/)

See exercise instructions and [`Makefile`](../Makefile) for running
applications.

## Create a chat app: Chat app

Updated file `chat-app.py` and added `pyproject.toml` and example `.env` file.

## Create generative AI using your data Retrieval Augmented Generation (RAG) app

Updated file with comments `rag-app.py` and added `pyproject.toml` and example
`.env` file.
