[project]
name = "image-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "dotenv",
  "matplotlib",
  "pillow",
  "azure-ai-vision-face==1.0.0b2",
]
