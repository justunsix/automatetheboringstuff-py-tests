[project]
name = "sound-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "transformers",
  "datasets",
  "torch",
  "soundfile",
  "sentencepiece",
]
